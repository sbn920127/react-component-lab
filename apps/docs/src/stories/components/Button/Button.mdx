import { Meta } from "@storybook/blocks";
import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

# Button 組件設計理念

這是一個 **Headless** 的按鈕組件，它剝離了原本繁重的樣式...

## 使用時機
- 表單提交
- 模態框操作
